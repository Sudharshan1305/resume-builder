apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: resume-uploads-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: hostpath  # CHANGED from 'standard' to 'hostpath'