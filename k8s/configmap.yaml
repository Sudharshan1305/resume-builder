apiVersion: v1
kind: ConfigMap
metadata:
  name: resume-builder-config
data:
  APP_NAME: "Resume Builder"
  APP_VERSION: "2.0.0"
  NODE_ENV: "production"
  LOG_LEVEL: "info"
  MAX_FILE_SIZE: "5242880"  # 5MB in bytes